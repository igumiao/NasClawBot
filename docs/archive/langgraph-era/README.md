# LangGraph-Era Archive

This directory contains historical LangGraph workflow documents for reference only.

The active codebase no longer uses LangGraph workflow routing. Current project guidance lives in `AGENTS.md` and `CLAUDE.md`.
